/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add any other Next.js specific configurations you need here.
  // For example:
  // reactStrictMode: true,
  // images: {
  //   domains: ['example.com'],
  // },
};

export default nextConfig;
